{
  // Wrangler config for the Worker that fronts the petdata FastAPI backend as a
  // Cloudflare Container (ADR 0008). The Worker is a thin router; all
  // application code lives in the container image and references no Cloudflare
  // primitives.
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "petdata",
  "main": "worker/index.ts",
  "compatibility_date": "2026-06-01",

  // Production hostname (ADR 0008). `custom_domain: true` has Cloudflare
  // provision the DNS record and edge route for this subdomain on the
  // `backchain.app` zone. Applying it needs a deploy token with Zone Workers
  // Routes + DNS edit on `backchain.app`; that token and the deploy itself are
  // the operator's step.
  "routes": [
    {
      "pattern": "petdata.evermore.backchain.app",
      "custom_domain": true
    }
  ],

  // One container, built from the existing multi-stage Dockerfile. `image` is
  // relative to this config's directory, so `./Dockerfile` is
  // services/petdata/Dockerfile. `image_build_context: "../.."` sets the Docker
  // build context to the repo root so the `packages/schema` path dependency
  // (services/petdata/pyproject.toml -> ../../packages/schema) sits inside the
  // context; without it `uv sync --frozen` cannot resolve the sibling and the
  // build fails. Consequently the Dockerfile's COPY paths are repo-root-relative
  // (e.g. `COPY services/petdata/src/ ...`).
  "containers": [
    {
      "class_name": "PetdataContainer",
      "image": "./Dockerfile",
      "image_build_context": "../..",
      "max_instances": 3,
      "instance_type": "standard-1"
    }
  ],

  // Cloudflare Containers are backed by a Durable Object. The container class is
  // bound here and registered as a new SQLite-backed class in the migration
  // below (both are required by Cloudflare Containers).
  "durable_objects": {
    "bindings": [
      {
        "class_name": "PetdataContainer",
        "name": "PETDATA_CONTAINER"
      }
    ]
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["PetdataContainer"]
    }
  ],

  // Non-secret environment. The container binds 0.0.0.0:${PORT}.
  // Secrets (PETDATA_DATABASE_URL, PETDATA_COOKIES, and the deployment-specific
  // PETDATA_SMS_* config) are injected via `wrangler secret put <NAME>` /
  // Wrangler secret bindings and forwarded into the container by
  // worker/index.ts. They are NEVER committed here.
  //
  // The container connects directly to the Supabase pooler via
  // PETDATA_DATABASE_URL. No Hyperdrive binding is configured: Hyperdrive in
  // front of Supabase is deferred to issue #109.
  //
  // PETDATA_ALLOWED_ORIGINS is the production CORS allow-list (not a secret).
  // The Python `allowed_origins: list[str]` field is parsed from this env var as
  // JSON by pydantic-settings, so the value is a JSON array, not a bare string.
  // worker/index.ts forwards it into the container; `allow_credentials=True`
  // forbids a `*` wildcard.
  "vars": {
    "PORT": "8000",
    "PETDATA_ALLOWED_ORIGINS": "[\"https://evermore.backchain.app\"]"
  },

  // Observability is declarative in wrangler config: each deploy resets it to
  // the value committed here, so logging must live in config to persist across
  // deploys (issue #145). Invocation logs surface container boot errors and
  // Python tracebacks in the dashboard Logs and `wrangler tail`. Traces off.
  "observability": {
    "logs": { "enabled": true, "invocation_logs": true },
    "traces": { "enabled": false }
  }
}
